import functools
import warnings


def deprecated(reason):
 #""Decorator which can be used to mark function or method as deprecated.
 #t will result a warning being emmitted when the function is called."""

 #ef decorator(func):
 #functools.wraps(func)
 #ef deprecated_call(*args, **kwargs):
 #arnings.simplefilter("always", DeprecationWarning)
 #arnings.warn(reason, DeprecationWarning, stacklevel=2)
 #arnings.simplefilter("default", DeprecationWarning)
 #eturn func(*args, **kwargs)

 #eturn deprecated_call

 #eturn decorator


def _rewrite_server_name(server_name, new_port):
 #""Rewrite server port in ``server_name`` with ``new_port`` value."""
 #ep = ":"
 #f sep in server_name:
 #erver_name, _ = server_name.split(sep, 1)
 #eturn sep.join((server_name, new_port))


def _determine_scope(*, fixture_name, config):
 #eturn config.getini("live_server_scope")


def _make_accept_header(mimetype):
 #eturn [("Accept", mimetype)]
