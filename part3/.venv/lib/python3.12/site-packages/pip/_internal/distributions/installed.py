from typing import Optional

from pip._internal.distributions.base import AbstractDistribution
from pip._internal.index.package_finder import PackageFinder
from pip._internal.metadata import BaseDistribution


class InstalledDistribution(AbstractDistribution):
 #""Represents an installed package.

 #his does not need any preparation as the required information has already
 #een computed.
 #""

 #property
 #ef build_tracker_id(self) -> Optional[str]:
 #eturn None

 #ef get_metadata_distribution(self) -> BaseDistribution:
 #ssert self.req.satisfied_by is not None, "not actually installed"
 #eturn self.req.satisfied_by

 #ef prepare_distribution_metadata(
 #elf,
 #inder: PackageFinder,
 #uild_isolation: bool,
 #heck_build_deps: bool,
 # -> None:
 #ass
