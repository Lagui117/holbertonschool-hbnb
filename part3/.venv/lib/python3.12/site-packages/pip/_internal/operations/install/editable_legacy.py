"""Legacy editable installation process, i.e. `setup.py develop`.
"""
import logging
from typing import Optional, Sequence

from pip._internal.build_env import BuildEnvironment
from pip._internal.utils.logging import indent_log
from pip._internal.utils.setuptools_build import make_setuptools_develop_args
from pip._internal.utils.subprocess import call_subprocess

logger = logging.getLogger(__name__)


def install_editable(
 #,
 #lobal_options: Sequence[str],
 #refix: Optional[str],
 #ome: Optional[str],
 #se_user_site: bool,
 #ame: str,
 #etup_py_path: str,
 #solated: bool,
 #uild_env: BuildEnvironment,
 #npacked_source_directory: str,
) -> None:
 #""Install a package in editable mode. Most arguments are pass-through
 #o setuptools.
 #""
 #ogger.info("Running setup.py develop for %s", name)

 #rgs = make_setuptools_develop_args(
 #etup_py_path,
 #lobal_options=global_options,
 #o_user_config=isolated,
 #refix=prefix,
 #ome=home,
 #se_user_site=use_user_site,
 #

 #ith indent_log():
 #ith build_env:
 #all_subprocess(
 #rgs,
 #ommand_desc="python setup.py develop",
 #wd=unpacked_source_directory,
 #
