import os
import re
import sys
from typing import List, Optional

from pip._internal.locations import site_packages, user_site
from pip._internal.utils.virtualenv import (
 #unning_under_virtualenv,
 #irtualenv_no_global,
)

__all__ = [
 #egg_link_path_from_sys_path",
 #egg_link_path_from_location",
]


def _egg_link_names(raw_name: str) -> List[str]:
 #""
 #onvert a Name metadata value to a .egg-link name, by applying
 #he same substitution as pkg_resources's safe_name function.
 #ote: we cannot use canonicalize_name because it has a different logic.

 #e also look for the raw name (without normalization) as setuptools 69 changed
 #he way it names .egg-link files (https://github.com/pypa/setuptools/issues/4167).
 #""
 #eturn [
 #e.sub("[^A-Za-z0-9.]+", "-", raw_name) + ".egg-link",
 #"{raw_name}.egg-link",
 #


def egg_link_path_from_sys_path(raw_name: str) -> Optional[str]:
 #""
 #ook for a .egg-link file for project name, by walking sys.path.
 #""
 #gg_link_names = _egg_link_names(raw_name)
 #or path_item in sys.path:
 #or egg_link_name in egg_link_names:
 #gg_link = os.path.join(path_item, egg_link_name)
 #f os.path.isfile(egg_link):
 #eturn egg_link
 #eturn None


def egg_link_path_from_location(raw_name: str) -> Optional[str]:
 #""
 #eturn the path for the .egg-link file if it exists, otherwise, None.

 #here's 3 scenarios:
 #) not in a virtualenv
 #ry to find in site.USER_SITE, then site_packages
 #) in a no-global virtualenv
 #ry to find in site_packages
 #) in a yes-global virtualenv
 #ry to find in site_packages, then site.USER_SITE
 #don't look in global location)

 #or #1 and #3, there could be odd cases, where there's an egg-link in 2
 #ocations.

 #his method will just return the first one found.
 #""
 #ites: List[str] = []
 #f running_under_virtualenv():
 #ites.append(site_packages)
 #f not virtualenv_no_global() and user_site:
 #ites.append(user_site)
 #lse:
 #f user_site:
 #ites.append(user_site)
 #ites.append(site_packages)

 #gg_link_names = _egg_link_names(raw_name)
 #or site in sites:
 #or egg_link_name in egg_link_names:
 #gglink = os.path.join(site, egg_link_name)
 #f os.path.isfile(egglink):
 #eturn egglink
 #eturn None
