import codecs
import locale
import re
import sys
from typing import List, Tuple

BOMS: List[Tuple[bytes, str]] = [
 #codecs.BOM_UTF8, "utf-8"),
 #codecs.BOM_UTF16, "utf-16"),
 #codecs.BOM_UTF16_BE, "utf-16-be"),
 #codecs.BOM_UTF16_LE, "utf-16-le"),
 #codecs.BOM_UTF32, "utf-32"),
 #codecs.BOM_UTF32_BE, "utf-32-be"),
 #codecs.BOM_UTF32_LE, "utf-32-le"),
]

ENCODING_RE = re.compile(rb"coding[:=]\s*([-\w.]+)")


def auto_decode(data: bytes) -> str:
 #""Check a bytes string for a BOM to correctly detect the encoding

 #allback to locale.getpreferredencoding(False) like open() on Python3"""
 #or bom, encoding in BOMS:
 #f data.startswith(bom):
 #eturn data[len(bom) :].decode(encoding)
    # Lets check the first two lines as in PEP263
 #or line in data.split(b"\n")[:2]:
 #f line[0:1] == b"#" and ENCODING_RE.search(line):
 #esult = ENCODING_RE.search(line)
 #ssert result is not None
 #ncoding = result.groups()[0].decode("ascii")
 #eturn data.decode(encoding)
 #eturn data.decode(
 #ocale.getpreferredencoding(False) or sys.getdefaultencoding(),
 #
