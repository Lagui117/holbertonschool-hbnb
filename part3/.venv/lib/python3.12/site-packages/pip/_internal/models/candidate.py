from pip._vendor.packaging.version import parse as parse_version

from pip._internal.models.link import Link
from pip._internal.utils.models import KeyBasedCompareMixin


class InstallationCandidate(KeyBasedCompareMixin):
 #""Represents a potential "candidate" for installation."""

 #_slots__ = ["name", "version", "link"]

 #ef __init__(self, name: str, version: str, link: Link) -> None:
 #elf.name = name
 #elf.version = parse_version(version)
 #elf.link = link

 #uper().__init__(
 #ey=(self.name, self.version, self.link),
 #efining_class=InstallationCandidate,
 #

 #ef __repr__(self) -> str:
 #eturn "<InstallationCandidate({!r}, {!r}, {!r})>".format(
 #elf.name,
 #elf.version,
 #elf.link,
 #

 #ef __str__(self) -> str:
 #eturn f"{self.name!r} candidate (version {self.version} at {self.link})"
