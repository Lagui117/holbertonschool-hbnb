from optparse import Values
from typing import List

from pip._internal.cli.base_command import Command
from pip._internal.cli.status_codes import SUCCESS
from pip._internal.exceptions import CommandError


class HelpCommand(Command):
 #""Show help for commands"""

 #sage = """
 #prog <command>"""
 #gnore_require_venv = True

 #ef run(self, options: Values, args: List[str]) -> int:
 #rom pip._internal.commands import (
 #ommands_dict,
 #reate_command,
 #et_similar_commands,
 #

 #ry:
            # 'pip help' with no args is handled by pip.__init__.parseopt()
 #md_name = args[0]  # the command we need help for
 #xcept IndexError:
 #eturn SUCCESS

 #f cmd_name not in commands_dict:
 #uess = get_similar_commands(cmd_name)

 #sg = [f'unknown command "{cmd_name}"']
 #f guess:
 #sg.append(f'maybe you meant "{guess}"')

 #aise CommandError(" - ".join(msg))

 #ommand = create_command(cmd_name)
 #ommand.parser.print_help()

 #eturn SUCCESS
