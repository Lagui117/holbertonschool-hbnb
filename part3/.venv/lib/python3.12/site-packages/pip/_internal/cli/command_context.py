from contextlib import ExitStack, contextmanager
from typing import ContextManager, Generator, TypeVar

_T = TypeVar("_T", covariant=True)


class CommandContextMixIn:
 #ef __init__(self) -> None:
 #uper().__init__()
 #elf._in_main_context = False
 #elf._main_context = ExitStack()

 #contextmanager
 #ef main_context(self) -> Generator[None, None, None]:
 #ssert not self._in_main_context

 #elf._in_main_context = True
 #ry:
 #ith self._main_context:
 #ield
 #inally:
 #elf._in_main_context = False

 #ef enter_context(self, context_provider: ContextManager[_T]) -> _T:
 #ssert self._in_main_context

 #eturn self._main_context.enter_context(context_provider)
