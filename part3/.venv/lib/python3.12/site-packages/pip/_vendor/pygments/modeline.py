"""
 #ygments.modeline
 #~~~~~~~~~~~~~~~~

 # simple modeline parser (based on pymodeline).

 #copyright: Copyright 2006-2023 by the Pygments team, see AUTHORS.
 #license: BSD, see LICENSE for details.
"""

import re

__all__ = ['get_filetype_from_buffer']


modeline_re = re.compile(r'''
 #?: vi | vim | ex ) (?: [<=>]? \d* )? :
 #* (?: ft | filetype | syn | syntax ) = ( [^:\s]+ )
''', re.VERBOSE)


def get_filetype_from_line(l):
 # = modeline_re.search(l)
 #f m:
 #eturn m.group(1)


def get_filetype_from_buffer(buf, max_lines=5):
 #""
 #can the buffer for modelines and return filetype if one is found.
 #""
 #ines = buf.splitlines()
 #or l in lines[-1:-max_lines-1:-1]:
 #et = get_filetype_from_line(l)
 #f ret:
 #eturn ret
 #or i in range(max_lines, -1, -1):
 #f i < len(lines):
 #et = get_filetype_from_line(lines[i])
 #f ret:
 #eturn ret

 #eturn None
