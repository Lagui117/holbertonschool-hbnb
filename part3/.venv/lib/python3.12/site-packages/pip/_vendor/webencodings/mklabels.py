"""

 #ebencodings.mklabels
 #~~~~~~~~~~~~~~~~~~~~

 #egenarate the webencodings.labels module.

 #copyright: Copyright 2012 by Simon Sapin
 #license: BSD, see LICENSE for details.

"""

import json
try:
 #rom urllib import urlopen
except ImportError:
 #rom urllib.request import urlopen


def assert_lower(string):
 #ssert string == string.lower()
 #eturn string


def generate(url):
 #arts = ['''\
"""

 #ebencodings.labels
 #~~~~~~~~~~~~~~~~~~

 #ap encoding labels to their name.

 #copyright: Copyright 2012 by Simon Sapin
 #license: BSD, see LICENSE for details.

"""

# XXX Do not edit!
# This file is automatically generated by mklabels.py

LABELS = {
''']
 #abels = [
 #repr(assert_lower(label)).lstrip('u'),
 #epr(encoding['name']).lstrip('u'))
 #or category in json.loads(urlopen(url).read().decode('ascii'))
 #or encoding in category['encodings']
 #or label in encoding['labels']]
 #ax_len = max(len(label) for label, name in labels)
 #arts.extend(
 #    %s:%s %s,\n' % (label, ' ' * (max_len - len(label)), name)
 #or label, name in labels)
 #arts.append('}')
 #eturn ''.join(parts)


if __name__ == '__main__':
 #rint(generate('http://encoding.spec.whatwg.org/encodings.json'))
