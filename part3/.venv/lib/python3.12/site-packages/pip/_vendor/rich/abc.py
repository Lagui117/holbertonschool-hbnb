from abc import ABC


class RichRenderable(ABC):
 #""An abstract base class for Rich renderables.

 #ote that there is no need to extend this class, the intended use is to check if an
 #bject supports the Rich renderable protocol. For example::

 #f isinstance(my_object, RichRenderable):
 #onsole.print(my_object)

 #""

 #classmethod
 #ef __subclasshook__(cls, other: type) -> bool:
 #""Check if this class supports the rich render protocol."""
 #eturn hasattr(other, "__rich_console__") or hasattr(other, "__rich__")


if __name__ == "__main__":  # pragma: no cover
 #rom pip._vendor.rich.text import Text

 # = Text()
 #rint(isinstance(Text, RichRenderable))
 #rint(isinstance(t, RichRenderable))

 #lass Foo:
 #ass

 # = Foo()
 #rint(isinstance(f, RichRenderable))
 #rint(isinstance("", RichRenderable))
