from typing import TYPE_CHECKING

from .measure import Measurement
from .segment import Segment
from .style import StyleType

if TYPE_CHECKING:
 #rom .console import Console, ConsoleOptions, RenderResult, RenderableType


class Styled:
 #""Apply a style to a renderable.

 #rgs:
 #enderable (RenderableType): Any renderable.
 #tyle (StyleType): A style to apply across the entire renderable.
 #""

 #ef __init__(self, renderable: "RenderableType", style: "StyleType") -> None:
 #elf.renderable = renderable
 #elf.style = style

 #ef __rich_console__(
 #elf, console: "Console", options: "ConsoleOptions"
 # -> "RenderResult":
 #tyle = console.get_style(self.style)
 #endered_segments = console.render(self.renderable, options)
 #egments = Segment.apply_style(rendered_segments, style)
 #eturn segments

 #ef __rich_measure__(
 #elf, console: "Console", options: "ConsoleOptions"
 # -> Measurement:
 #eturn Measurement.get(console, options, self.renderable)


if __name__ == "__main__":  # pragma: no cover
 #rom pip._vendor.rich import print
 #rom pip._vendor.rich.panel import Panel

 #anel = Styled(Panel("hello"), "on blue")
 #rint(panel)
