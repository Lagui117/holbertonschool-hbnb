from abc import ABC, abstractmethod
from typing import Any


class Pager(ABC):
 #""Base class for a pager."""

 #abstractmethod
 #ef show(self, content: str) -> None:
 #""Show content in pager.

 #rgs:
 #ontent (str): Content to be displayed.
 #""


class SystemPager(Pager):
 #""Uses the pager installed on the system."""

 #ef _pager(self, content: str) -> Any:  #  pragma: no cover
 #eturn __import__("pydoc").pager(content)

 #ef show(self, content: str) -> None:
 #""Use the same pager used by pydoc."""
 #elf._pager(content)


if __name__ == "__main__":  # pragma: no cover
 #rom .__main__ import make_test_card
 #rom .console import Console

 #onsole = Console()
 #ith console.pager(styles=True):
 #onsole.print(make_test_card())
