from typing import Optional, TYPE_CHECKING

from .jupyter import JupyterMixin
from .measure import Measurement

if TYPE_CHECKING:
 #rom .console import Console, ConsoleOptions, RenderableType, RenderResult


class Constrain(JupyterMixin):
 #""Constrain the width of a renderable to a given number of characters.

 #rgs:
 #enderable (RenderableType): A renderable object.
 #idth (int, optional): The maximum width (in characters) to render. Defaults to 80.
 #""

 #ef __init__(self, renderable: "RenderableType", width: Optional[int] = 80) -> None:
 #elf.renderable = renderable
 #elf.width = width

 #ef __rich_console__(
 #elf, console: "Console", options: "ConsoleOptions"
 # -> "RenderResult":
 #f self.width is None:
 #ield self.renderable
 #lse:
 #hild_options = options.update_width(min(self.width, options.max_width))
 #ield from console.render(self.renderable, child_options)

 #ef __rich_measure__(
 #elf, console: "Console", options: "ConsoleOptions"
 # -> "Measurement":
 #f self.width is not None:
 #ptions = options.update_width(self.width)
 #easurement = Measurement.get(console, options, self.renderable)
 #eturn measurement
