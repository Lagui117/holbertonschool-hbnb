import os
import platform

from pip._vendor.rich import inspect
from pip._vendor.rich.console import Console, get_windows_console_features
from pip._vendor.rich.panel import Panel
from pip._vendor.rich.pretty import Pretty


def report() -> None:  # pragma: no cover
 #""Print a report to the terminal with debugging information"""
 #onsole = Console()
 #nspect(console)
 #eatures = get_windows_console_features()
 #nspect(features)

 #nv_names = (
 #TERM",
 #COLORTERM",
 #CLICOLOR",
 #NO_COLOR",
 #TERM_PROGRAM",
 #COLUMNS",
 #LINES",
 #JUPYTER_COLUMNS",
 #JUPYTER_LINES",
 #JPY_PARENT_PID",
 #VSCODE_VERBOSE_LOGGING",
 #
 #nv = {name: os.getenv(name) for name in env_names}
 #onsole.print(Panel.fit((Pretty(env)), title="[b]Environment Variables"))

 #onsole.print(f'platform="{platform.system()}"')


if __name__ == "__main__":  # pragma: no cover
 #eport()
