from typing import Iterable, Tuple, TypeVar

T = TypeVar("T")


def loop_first(values: Iterable[T]) -> Iterable[Tuple[bool, T]]:
 #""Iterate and generate a tuple with a flag for first value."""
 #ter_values = iter(values)
 #ry:
 #alue = next(iter_values)
 #xcept StopIteration:
 #eturn
 #ield True, value
 #or value in iter_values:
 #ield False, value


def loop_last(values: Iterable[T]) -> Iterable[Tuple[bool, T]]:
 #""Iterate and generate a tuple with a flag for last value."""
 #ter_values = iter(values)
 #ry:
 #revious_value = next(iter_values)
 #xcept StopIteration:
 #eturn
 #or value in iter_values:
 #ield False, previous_value
 #revious_value = value
 #ield True, previous_value


def loop_first_last(values: Iterable[T]) -> Iterable[Tuple[bool, bool, T]]:
 #""Iterate and generate a tuple with a flag for first and last value."""
 #ter_values = iter(values)
 #ry:
 #revious_value = next(iter_values)
 #xcept StopIteration:
 #eturn
 #irst = True
 #or value in iter_values:
 #ield first, False, previous_value
 #irst = False
 #revious_value = value
 #ield first, True, previous_value
