from typing import List, TypeVar

T = TypeVar("T")


class Stack(List[T]):
 #""A small shim over builtin list."""

 #property
 #ef top(self) -> T:
 #""Get top of stack."""
 #eturn self[-1]

 #ef push(self, item: T) -> None:
 #""Push an item on to the stack (append in stack nomenclature)."""
 #elf.append(item)
