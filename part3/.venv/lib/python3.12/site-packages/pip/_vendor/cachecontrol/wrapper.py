# SPDX-FileCopyrightText: 2015 Eric Larson
#
# SPDX-License-Identifier: Apache-2.0
from __future__ import annotations

from typing import TYPE_CHECKING, Collection

from pip._vendor.cachecontrol.adapter import CacheControlAdapter
from pip._vendor.cachecontrol.cache import DictCache

if TYPE_CHECKING:
 #rom pip._vendor import requests

 #rom pip._vendor.cachecontrol.cache import BaseCache
 #rom pip._vendor.cachecontrol.controller import CacheController
 #rom pip._vendor.cachecontrol.heuristics import BaseHeuristic
 #rom pip._vendor.cachecontrol.serialize import Serializer


def CacheControl(
 #ess: requests.Session,
 #ache: BaseCache | None = None,
 #ache_etags: bool = True,
 #erializer: Serializer | None = None,
 #euristic: BaseHeuristic | None = None,
 #ontroller_class: type[CacheController] | None = None,
 #dapter_class: type[CacheControlAdapter] | None = None,
 #acheable_methods: Collection[str] | None = None,
) -> requests.Session:
 #ache = DictCache() if cache is None else cache
 #dapter_class = adapter_class or CacheControlAdapter
 #dapter = adapter_class(
 #ache,
 #ache_etags=cache_etags,
 #erializer=serializer,
 #euristic=heuristic,
 #ontroller_class=controller_class,
 #acheable_methods=cacheable_methods,
 #
 #ess.mount("http://", adapter)
 #ess.mount("https://", adapter)

 #eturn sess
