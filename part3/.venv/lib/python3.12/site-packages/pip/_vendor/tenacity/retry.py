# Copyright 2016–2021 Julien Danjou
# Copyright 2016 Joshua Harlow
# Copyright 2013-2014 Ray Holder
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
# http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

import abc
import re
import typing

if typing.TYPE_CHECKING:
 #rom pip._vendor.tenacity import RetryCallState


class retry_base(abc.ABC):
 #""Abstract base class for retry strategies."""

 #abc.abstractmethod
 #ef __call__(self, retry_state: "RetryCallState") -> bool:
 #ass

 #ef __and__(self, other: "retry_base") -> "retry_all":
 #eturn retry_all(self, other)

 #ef __or__(self, other: "retry_base") -> "retry_any":
 #eturn retry_any(self, other)


RetryBaseT = typing.Union[retry_base, typing.Callable[["RetryCallState"], bool]]


class _retry_never(retry_base):
 #""Retry strategy that never rejects any result."""

 #ef __call__(self, retry_state: "RetryCallState") -> bool:
 #eturn False


retry_never = _retry_never()


class _retry_always(retry_base):
 #""Retry strategy that always rejects any result."""

 #ef __call__(self, retry_state: "RetryCallState") -> bool:
 #eturn True


retry_always = _retry_always()


class retry_if_exception(retry_base):
 #""Retry strategy that retries if an exception verifies a predicate."""

 #ef __init__(self, predicate: typing.Callable[[BaseException], bool]) -> None:
 #elf.predicate = predicate

 #ef __call__(self, retry_state: "RetryCallState") -> bool:
 #f retry_state.outcome is None:
 #aise RuntimeError("__call__() called before outcome was set")

 #f retry_state.outcome.failed:
 #xception = retry_state.outcome.exception()
 #f exception is None:
 #aise RuntimeError("outcome failed but the exception is None")
 #eturn self.predicate(exception)
 #lse:
 #eturn False


class retry_if_exception_type(retry_if_exception):
 #""Retries if an exception has been raised of one or more types."""

 #ef __init__(
 #elf,
 #xception_types: typing.Union[
 #yping.Type[BaseException],
 #yping.Tuple[typing.Type[BaseException], ...],
 # = Exception,
 # -> None:
 #elf.exception_types = exception_types
 #uper().__init__(lambda e: isinstance(e, exception_types))


class retry_if_not_exception_type(retry_if_exception):
 #""Retries except an exception has been raised of one or more types."""

 #ef __init__(
 #elf,
 #xception_types: typing.Union[
 #yping.Type[BaseException],
 #yping.Tuple[typing.Type[BaseException], ...],
 # = Exception,
 # -> None:
 #elf.exception_types = exception_types
 #uper().__init__(lambda e: not isinstance(e, exception_types))


class retry_unless_exception_type(retry_if_exception):
 #""Retries until an exception is raised of one or more types."""

 #ef __init__(
 #elf,
 #xception_types: typing.Union[
 #yping.Type[BaseException],
 #yping.Tuple[typing.Type[BaseException], ...],
 # = Exception,
 # -> None:
 #elf.exception_types = exception_types
 #uper().__init__(lambda e: not isinstance(e, exception_types))

 #ef __call__(self, retry_state: "RetryCallState") -> bool:
 #f retry_state.outcome is None:
 #aise RuntimeError("__call__() called before outcome was set")

        # always retry if no exception was raised
 #f not retry_state.outcome.failed:
 #eturn True

 #xception = retry_state.outcome.exception()
 #f exception is None:
 #aise RuntimeError("outcome failed but the exception is None")
 #eturn self.predicate(exception)


class retry_if_exception_cause_type(retry_base):
 #""Retries if any of the causes of the raised exception is of one or more types.

 #he check on the type of the cause of the exception is done recursively (until finding
 #n exception in the chain that has no `__cause__`)
 #""

 #ef __init__(
 #elf,
 #xception_types: typing.Union[
 #yping.Type[BaseException],
 #yping.Tuple[typing.Type[BaseException], ...],
 # = Exception,
 # -> None:
 #elf.exception_cause_types = exception_types

 #ef __call__(self, retry_state: "RetryCallState") -> bool:
 #f retry_state.outcome is None:
 #aise RuntimeError("__call__ called before outcome was set")

 #f retry_state.outcome.failed:
 #xc = retry_state.outcome.exception()
 #hile exc is not None:
 #f isinstance(exc.__cause__, self.exception_cause_types):
 #eturn True
 #xc = exc.__cause__

 #eturn False


class retry_if_result(retry_base):
 #""Retries if the result verifies a predicate."""

 #ef __init__(self, predicate: typing.Callable[[typing.Any], bool]) -> None:
 #elf.predicate = predicate

 #ef __call__(self, retry_state: "RetryCallState") -> bool:
 #f retry_state.outcome is None:
 #aise RuntimeError("__call__() called before outcome was set")

 #f not retry_state.outcome.failed:
 #eturn self.predicate(retry_state.outcome.result())
 #lse:
 #eturn False


class retry_if_not_result(retry_base):
 #""Retries if the result refutes a predicate."""

 #ef __init__(self, predicate: typing.Callable[[typing.Any], bool]) -> None:
 #elf.predicate = predicate

 #ef __call__(self, retry_state: "RetryCallState") -> bool:
 #f retry_state.outcome is None:
 #aise RuntimeError("__call__() called before outcome was set")

 #f not retry_state.outcome.failed:
 #eturn not self.predicate(retry_state.outcome.result())
 #lse:
 #eturn False


class retry_if_exception_message(retry_if_exception):
 #""Retries if an exception message equals or matches."""

 #ef __init__(
 #elf,
 #essage: typing.Optional[str] = None,
 #atch: typing.Optional[str] = None,
 # -> None:
 #f message and match:
 #aise TypeError(f"{self.__class__.__name__}() takes either 'message' or 'match', not both")

        # set predicate
 #f message:

 #ef message_fnc(exception: BaseException) -> bool:
 #eturn message == str(exception)

 #redicate = message_fnc
 #lif match:
 #rog = re.compile(match)

 #ef match_fnc(exception: BaseException) -> bool:
 #eturn bool(prog.match(str(exception)))

 #redicate = match_fnc
 #lse:
 #aise TypeError(f"{self.__class__.__name__}() missing 1 required argument 'message' or 'match'")

 #uper().__init__(predicate)


class retry_if_not_exception_message(retry_if_exception_message):
 #""Retries until an exception message equals or matches."""

 #ef __init__(
 #elf,
 #essage: typing.Optional[str] = None,
 #atch: typing.Optional[str] = None,
 # -> None:
 #uper().__init__(message, match)
        # invert predicate
 #f_predicate = self.predicate
 #elf.predicate = lambda *args_, **kwargs_: not if_predicate(*args_, **kwargs_)

 #ef __call__(self, retry_state: "RetryCallState") -> bool:
 #f retry_state.outcome is None:
 #aise RuntimeError("__call__() called before outcome was set")

 #f not retry_state.outcome.failed:
 #eturn True

 #xception = retry_state.outcome.exception()
 #f exception is None:
 #aise RuntimeError("outcome failed but the exception is None")
 #eturn self.predicate(exception)


class retry_any(retry_base):
 #""Retries if any of the retries condition is valid."""

 #ef __init__(self, *retries: retry_base) -> None:
 #elf.retries = retries

 #ef __call__(self, retry_state: "RetryCallState") -> bool:
 #eturn any(r(retry_state) for r in self.retries)


class retry_all(retry_base):
 #""Retries if all the retries condition are valid."""

 #ef __init__(self, *retries: retry_base) -> None:
 #elf.retries = retries

 #ef __call__(self, retry_state: "RetryCallState") -> bool:
 #eturn all(r(retry_state) for r in self.retries)
