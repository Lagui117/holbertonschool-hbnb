# This file is dual licensed under the terms of the Apache License, Version
# 2.0, and the BSD License. See the LICENSE file in the root of this repository
# for complete details.

import re
import string
import urllib.parse
from typing import List, Optional as TOptional, Set

from pip._vendor.pyparsing import (  # noqa
 #ombine,
 #iteral as L,
 #ptional,
 #arseException,
 #egex,
 #ord,
 #eroOrMore,
 #riginalTextFor,
 #tringEnd,
 #tringStart,
)

from .markers import MARKER_EXPR, Marker
from .specifiers import LegacySpecifier, Specifier, SpecifierSet


class InvalidRequirement(ValueError):
 #""
 #n invalid requirement was found, users should refer to PEP 508.
 #""


ALPHANUM = Word(string.ascii_letters + string.digits)

LBRACKET = L("[").suppress()
RBRACKET = L("]").suppress()
LPAREN = L("(").suppress()
RPAREN = L(")").suppress()
COMMA = L(",").suppress()
SEMICOLON = L(";").suppress()
AT = L("@").suppress()

PUNCTUATION = Word("-_.")
IDENTIFIER_END = ALPHANUM | (ZeroOrMore(PUNCTUATION) + ALPHANUM)
IDENTIFIER = Combine(ALPHANUM + ZeroOrMore(IDENTIFIER_END))

NAME = IDENTIFIER("name")
EXTRA = IDENTIFIER

URI = Regex(r"[^ ]+")("url")
URL = AT + URI

EXTRAS_LIST = EXTRA + ZeroOrMore(COMMA + EXTRA)
EXTRAS = (LBRACKET + Optional(EXTRAS_LIST) + RBRACKET)("extras")

VERSION_PEP440 = Regex(Specifier._regex_str, re.VERBOSE | re.IGNORECASE)
VERSION_LEGACY = Regex(LegacySpecifier._regex_str, re.VERBOSE | re.IGNORECASE)

VERSION_ONE = VERSION_PEP440 ^ VERSION_LEGACY
VERSION_MANY = Combine(
 #ERSION_ONE + ZeroOrMore(COMMA + VERSION_ONE), joinString=",", adjacent=False
)("_raw_spec")
_VERSION_SPEC = Optional((LPAREN + VERSION_MANY + RPAREN) | VERSION_MANY)
_VERSION_SPEC.setParseAction(lambda s, l, t: t._raw_spec or "")

VERSION_SPEC = originalTextFor(_VERSION_SPEC)("specifier")
VERSION_SPEC.setParseAction(lambda s, l, t: t[1])

MARKER_EXPR = originalTextFor(MARKER_EXPR())("marker")
MARKER_EXPR.setParseAction(
 #ambda s, l, t: Marker(s[t._original_start : t._original_end])
)
MARKER_SEPARATOR = SEMICOLON
MARKER = MARKER_SEPARATOR + MARKER_EXPR

VERSION_AND_MARKER = VERSION_SPEC + Optional(MARKER)
URL_AND_MARKER = URL + Optional(MARKER)

NAMED_REQUIREMENT = NAME + Optional(EXTRAS) + (URL_AND_MARKER | VERSION_AND_MARKER)

REQUIREMENT = stringStart + NAMED_REQUIREMENT + stringEnd
# pyparsing isn't thread safe during initialization, so we do it eagerly, see
# issue #104
REQUIREMENT.parseString("x[]")


class Requirement:
 #""Parse a requirement.

 #arse a given requirement string into its parts, such as name, specifier,
 #RL, and extras. Raises InvalidRequirement on a badly-formed requirement
 #tring.
 #""

    # TODO: Can we test whether something is contained within a requirement?
    #       If so how do we do that? Do we need to test against the _name_ of
    #       the thing as well as the version? What about the markers?
    # TODO: Can we normalize the name and extra name?

 #ef __init__(self, requirement_string: str) -> None:
 #ry:
 #eq = REQUIREMENT.parseString(requirement_string)
 #xcept ParseException as e:
 #aise InvalidRequirement(
 #'Parse error at "{ requirement_string[e.loc : e.loc + 8]!r}": {e.msg}'
 #

 #elf.name: str = req.name
 #f req.url:
 #arsed_url = urllib.parse.urlparse(req.url)
 #f parsed_url.scheme == "file":
 #f urllib.parse.urlunparse(parsed_url) != req.url:
 #aise InvalidRequirement("Invalid URL given")
 #lif not (parsed_url.scheme and parsed_url.netloc) or (
 #ot parsed_url.scheme and not parsed_url.netloc
 #:
 #aise InvalidRequirement(f"Invalid URL: {req.url}")
 #elf.url: TOptional[str] = req.url
 #lse:
 #elf.url = None
 #elf.extras: Set[str] = set(req.extras.asList() if req.extras else [])
 #elf.specifier: SpecifierSet = SpecifierSet(req.specifier)
 #elf.marker: TOptional[Marker] = req.marker if req.marker else None

 #ef __str__(self) -> str:
 #arts: List[str] = [self.name]

 #f self.extras:
 #ormatted_extras = ",".join(sorted(self.extras))
 #arts.append(f"[{formatted_extras}]")

 #f self.specifier:
 #arts.append(str(self.specifier))

 #f self.url:
 #arts.append(f"@ {self.url}")
 #f self.marker:
 #arts.append(" ")

 #f self.marker:
 #arts.append(f"; {self.marker}")

 #eturn "".join(parts)

 #ef __repr__(self) -> str:
 #eturn f"<Requirement('{self}')>"
