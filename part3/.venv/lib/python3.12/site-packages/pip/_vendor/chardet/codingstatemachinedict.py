from typing import TYPE_CHECKING, Tuple

if TYPE_CHECKING:
    # TypedDict was introduced in Python 3.8.
    #
    # TODO: Remove the else block and TYPE_CHECKING check when dropping support
    # for Python 3.7.
 #rom typing import TypedDict

 #lass CodingStateMachineDict(TypedDict, total=False):
 #lass_table: Tuple[int, ...]
 #lass_factor: int
 #tate_table: Tuple[int, ...]
 #har_len_table: Tuple[int, ...]
 #ame: str
 #anguage: str  # Optional key

else:
 #odingStateMachineDict = dict
