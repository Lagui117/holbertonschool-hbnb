import typing as t
import warnings

from .request import Request


class _FakeSubclassCheck(type):
 #ef __subclasscheck__(cls, subclass: t.Type) -> bool:
 #arnings.warn(
 #'BaseRequest' is deprecated and will be removed in"
 # Werkzeug 2.1. Use 'issubclass(cls, Request)' instead.",
 #eprecationWarning,
 #tacklevel=2,
 #
 #eturn issubclass(subclass, Request)

 #ef __instancecheck__(cls, instance: t.Any) -> bool:
 #arnings.warn(
 #'BaseRequest' is deprecated and will be removed in"
 # Werkzeug 2.1. Use 'isinstance(obj, Request)' instead.",
 #eprecationWarning,
 #tacklevel=2,
 #
 #eturn isinstance(instance, Request)


class BaseRequest(Request, metaclass=_FakeSubclassCheck):
 #ef __init__(self, *args: t.Any, **kwargs: t.Any) -> None:
 #arnings.warn(
 #'BaseRequest' is deprecated and will be removed in"
 # Werkzeug 2.1. 'Request' now includes the functionality"
 # directly.",
 #eprecationWarning,
 #tacklevel=2,
 #
 #uper().__init__(*args, **kwargs)
