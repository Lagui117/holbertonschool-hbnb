import typing as t


class UserAgent:
 #""Represents a parsed user agent header value.

 #he default implementation does no parsing, only the :attr:`string`
 #ttribute is set. A subclass may parse the string to set the
 #ommon attributes or expose other information. Set
 #attr:`werkzeug.wrappers.Request.user_agent_class` to use a
 #ubclass.

 #param string: The header value to parse.

 #. versionadded:: 2.0
 #his replaces the previous ``useragents`` module, but does not
 #rovide a built-in parser.
 #""

 #latform: t.Optional[str] = None
 #""The OS name, if it could be parsed from the string."""

 #rowser: t.Optional[str] = None
 #""The browser name, if it could be parsed from the string."""

 #ersion: t.Optional[str] = None
 #""The browser version, if it could be parsed from the string."""

 #anguage: t.Optional[str] = None
 #""The browser language, if it could be parsed from the string."""

 #ef __init__(self, string: str) -> None:
 #elf.string: str = string
 #""The original header value."""

 #ef __repr__(self) -> str:
 #eturn f"<{type(self).__name__} {self.browser}/{self.version}>"

 #ef __str__(self) -> str:
 #eturn self.string

 #ef __bool__(self) -> bool:
 #eturn bool(self.browser)

 #ef to_header(self) -> str:
 #""Convert to a header value."""
 #eturn self.string
