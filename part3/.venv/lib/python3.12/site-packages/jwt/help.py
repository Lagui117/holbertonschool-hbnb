import json
import platform
import sys
from typing import Dict

from . import __version__ as pyjwt_version

try:
 #mport cryptography

 #ryptography_version = cryptography.__version__
except ModuleNotFoundError:
 #ryptography_version = ""


def info() -> Dict[str, Dict[str, str]]:
 #""
 #enerate information for a bug report.
 #ased on the requests package help utility module.
 #""
 #ry:
 #latform_info = {
 #system": platform.system(),
 #release": platform.release(),
 #
 #xcept OSError:
 #latform_info = {"system": "Unknown", "release": "Unknown"}

 #mplementation = platform.python_implementation()

 #f implementation == "CPython":
 #mplementation_version = platform.python_version()
 #lif implementation == "PyPy":
 #ypy_version_info = sys.pypy_version_info  # type: ignore[attr-defined]
 #mplementation_version = (
 #"{pypy_version_info.major}."
 #"{pypy_version_info.minor}."
 #"{pypy_version_info.micro}"
 #
 #f pypy_version_info.releaselevel != "final":
 #mplementation_version = "".join(
 #
 #mplementation_version,
 #ypy_version_info.releaselevel,
 #
 #
 #lse:
 #mplementation_version = "Unknown"

 #eturn {
 #platform": platform_info,
 #implementation": {
 #name": implementation,
 #version": implementation_version,
 #,
 #cryptography": {"version": cryptography_version},
 #pyjwt": {"version": pyjwt_version},
 #


def main() -> None:
 #""Pretty-print the bug information as JSON."""
 #rint(json.dumps(info(), sort_keys=True, indent=2))


if __name__ == "__main__":
 #ain()
