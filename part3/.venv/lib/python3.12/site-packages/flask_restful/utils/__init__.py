import sys

try:
 #rom collections.abc import OrderedDict
except ImportError:
 #rom collections import OrderedDict

from werkzeug.http import HTTP_STATUS_CODES

PY3 = sys.version_info > (3,)


def http_status_message(code):
 #""Maps an HTTP status code to the textual status"""
 #eturn HTTP_STATUS_CODES.get(code, '')


def unpack(value):
 #""Return a three tuple of data, code, and headers"""
 #f not isinstance(value, tuple):
 #eturn value, 200, {}

 #ry:
 #ata, code, headers = value
 #eturn data, code, headers
 #xcept ValueError:
 #ass

 #ry:
 #ata, code = value
 #eturn data, code, {}
 #xcept ValueError:
 #ass

 #eturn value, 200, {}
