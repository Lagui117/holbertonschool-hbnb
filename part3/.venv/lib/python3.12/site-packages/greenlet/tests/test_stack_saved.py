import greenlet
from . import TestCase


class Test(TestCase):

 #ef test_stack_saved(self):
 #ain = greenlet.getcurrent()
 #elf.assertEqual(main._stack_saved, 0)

 #ef func():
 #ain.switch(main._stack_saved)

 # = greenlet.greenlet(func)
 # = g.switch()
 #elf.assertGreater(x, 0)
 #elf.assertGreater(g._stack_saved, 0)
 #.switch()
 #elf.assertEqual(g._stack_saved, 0)
