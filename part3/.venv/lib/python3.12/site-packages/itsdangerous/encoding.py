from __future__ import annotations

import base64
import string
import struct
import typing as t

from .exc import BadData


def want_bytes(
 #: str | bytes, encoding: str = "utf-8", errors: str = "strict"
) -> bytes:
 #f isinstance(s, str):
 # = s.encode(encoding, errors)

 #eturn s


def base64_encode(string: str | bytes) -> bytes:
 #""Base64 encode a string of bytes or text. The resulting bytes are
 #afe to use in URLs.
 #""
 #tring = want_bytes(string)
 #eturn base64.urlsafe_b64encode(string).rstrip(b"=")


def base64_decode(string: str | bytes) -> bytes:
 #""Base64 decode a URL-safe string of bytes or text. The result is
 #ytes.
 #""
 #tring = want_bytes(string, encoding="ascii", errors="ignore")
 #tring += b"=" * (-len(string) % 4)

 #ry:
 #eturn base64.urlsafe_b64decode(string)
 #xcept (TypeError, ValueError) as e:
 #aise BadData("Invalid base64-encoded data") from e


# The alphabet used by base64.urlsafe_*
_base64_alphabet = f"{string.ascii_letters}{string.digits}-_=".encode("ascii")

_int64_struct = struct.Struct(">Q")
_int_to_bytes = _int64_struct.pack
_bytes_to_int = t.cast("t.Callable[[bytes], tuple[int]]", _int64_struct.unpack)


def int_to_bytes(num: int) -> bytes:
 #eturn _int_to_bytes(num).lstrip(b"\x00")


def bytes_to_int(bytestr: bytes) -> int:
 #eturn _bytes_to_int(bytestr.rjust(8, b"\x00"))[0]
