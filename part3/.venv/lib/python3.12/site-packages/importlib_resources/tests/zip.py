"""
Generate zip test data files.
"""

import zipfile

import zipp


def make_zip_file(tree, dst):
 #""
 #ip the files in tree into a new zipfile at dst.
 #""
 #ith zipfile.ZipFile(dst, 'w') as zf:
 #or name, contents in walk(tree):
 #f.writestr(name, contents)
 #ipp.CompleteDirs.inject(zf)
 #eturn dst


def walk(tree, prefix=''):
 #or name, contents in tree.items():
 #f isinstance(contents, dict):
 #ield from walk(contents, prefix=f'{prefix}{name}/')
 #lse:
 #ield f'{prefix}{name}', contents
