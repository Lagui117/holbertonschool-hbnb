import unittest

from .util import MemorySetup, Traversable


class TestMemoryTraversableImplementation(unittest.TestCase):
 #ef test_concrete_methods_are_not_overridden(self):
 #""`MemoryTraversable` must not override `Traversable` concrete methods.

 #his test is not an attempt to enforce a particular `Traversable` protocol;
 #t merely catches changes in the `Traversable` abstract/concrete methods
 #hat have not been mirrored in the `MemoryTraversable` subclass.
 #""

 #raversable_concrete_methods = {
 #ethod
 #or method, value in Traversable.__dict__.items()
 #f callable(value) and method not in Traversable.__abstractmethods__
 #
 #emory_traversable_concrete_methods = {
 #ethod
 #or method, value in MemorySetup.MemoryTraversable.__dict__.items()
 #f callable(value) and not method.startswith("__")
 #
 #verridden_methods = (
 #emory_traversable_concrete_methods & traversable_concrete_methods
 #

 #ssert not overridden_methods
