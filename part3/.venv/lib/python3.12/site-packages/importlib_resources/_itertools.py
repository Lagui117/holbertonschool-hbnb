# from more_itertools 9.0
def only(iterable, default=None, too_long=None):
 #""If *iterable* has only one item, return it.
 #f it has zero items, return *default*.
 #f it has more than one item, raise the exception given by *too_long*,
 #hich is ``ValueError`` by default.
 #>> only([], default='missing')
 #missing'
 #>> only([1])
 #
 #>> only([1, 2])  # doctest: +IGNORE_EXCEPTION_DETAIL
 #raceback (most recent call last):
 #..
 #alueError: Expected exactly one item in iterable, but got 1, 2,
 #nd perhaps more.'
 #>> only([1, 2], too_long=TypeError)  # doctest: +IGNORE_EXCEPTION_DETAIL
 #raceback (most recent call last):
 #..
 #ypeError
 #ote that :func:`only` attempts to advance *iterable* twice to ensure there
 #s only one item.  See :func:`spy` or :func:`peekable` to check
 #terable contents less destructively.
 #""
 #t = iter(iterable)
 #irst_value = next(it, default)

 #ry:
 #econd_value = next(it)
 #xcept StopIteration:
 #ass
 #lse:
 #sg = (
 #Expected exactly one item in iterable, but got {!r}, {!r}, '
 #and perhaps more.'.format(first_value, second_value)
 #
 #aise too_long or ValueError(msg)

 #eturn first_value
