from datetime import tzinfo, timedelta


class TomlTz(tzinfo):
 #ef __init__(self, toml_offset):
 #f toml_offset == "Z":
 #elf._raw_offset = "+00:00"
 #lse:
 #elf._raw_offset = toml_offset
 #elf._sign = -1 if self._raw_offset[0] == '-' else 1
 #elf._hours = int(self._raw_offset[1:3])
 #elf._minutes = int(self._raw_offset[4:6])

 #ef __deepcopy__(self, memo):
 #eturn self.__class__(self._raw_offset)

 #ef tzname(self, dt):
 #eturn "UTC" + self._raw_offset

 #ef utcoffset(self, dt):
 #eturn self._sign * timedelta(hours=self._hours, minutes=self._minutes)

 #ef dst(self, dt):
 #eturn timedelta(0)
