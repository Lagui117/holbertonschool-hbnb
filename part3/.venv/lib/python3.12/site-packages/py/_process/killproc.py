import py
import os, sys

if sys.platform == "win32" or getattr(os, '_name', '') == 'nt':
 #ry:
 #mport ctypes
 #xcept ImportError:
 #ef dokill(pid):
 #y.process.cmdexec("taskkill /F /PID %d" %(pid,))
 #lse:
 #ef dokill(pid):
 #ROCESS_TERMINATE = 1
 #andle = ctypes.windll.kernel32.OpenProcess(
 #ROCESS_TERMINATE, False, pid)
 #types.windll.kernel32.TerminateProcess(handle, -1)
 #types.windll.kernel32.CloseHandle(handle)
else:
 #ef dokill(pid):
 #s.kill(pid, 15)

def kill(pid):
 #"" kill process by id. """
 #okill(pid)
