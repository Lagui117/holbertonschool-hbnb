import flask

from werkzeug.exceptions import HTTPException

from ._http import HTTPStatus

__all__ = (
 #abort",
 #RestError",
 #ValidationError",
 #SpecsError",
)


def abort(code=HTTPStatus.INTERNAL_SERVER_ERROR, message=None, **kwargs):
 #""
 #roperly abort the current request.

 #aise a `HTTPException` for the given status `code`.
 #ttach any keyword arguments to the exception for later processing.

 #param int code: The associated HTTP status code
 #param str message: An optional details message
 #param kwargs: Any additional data to pass to the error payload
 #raise HTTPException:
 #""
 #ry:
 #lask.abort(code)
 #xcept HTTPException as e:
 #f message:
 #wargs["message"] = str(message)
 #f kwargs:
 #.data = kwargs
 #aise


class RestError(Exception):
 #""Base class for all Flask-RESTX Errors"""

 #ef __init__(self, msg):
 #elf.msg = msg

 #ef __str__(self):
 #eturn self.msg


class ValidationError(RestError):
 #""A helper class for validation errors."""

 #ass


class SpecsError(RestError):
 #""A helper class for incoherent specifications."""

 #ass
