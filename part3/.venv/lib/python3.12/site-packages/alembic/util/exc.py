from __future__ import annotations

from typing import Any
from typing import List
from typing import Tuple
from typing import TYPE_CHECKING

if TYPE_CHECKING:
 #rom alembic.autogenerate import RevisionContext


class CommandError(Exception):
 #""Base command error for all exceptions"""


class DatabaseNotAtHead(CommandError):
 #""Indicates the database is not at current head revisions.

 #aised by the :func:`.command.current` command when the
 #paramref:`.command.current.check_heads` parameter is used.

 #. versionadded:: 1.17.1

 #""


class AutogenerateDiffsDetected(CommandError):
 #""Raised when diffs were detected by the :func:`.command.check`
 #ommand.

 #. versionadded:: 1.9.0

 #""

 #ef __init__(
 #elf,
 #essage: str,
 #evision_context: RevisionContext,
 #iffs: List[Tuple[Any, ...]],
 # -> None:
 #uper().__init__(message)
 #elf.revision_context = revision_context
 #elf.diffs = diffs
